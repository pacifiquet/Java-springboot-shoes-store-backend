import {ProductStateStateInterface} from './types/ProductInterface';

export const productListInitialState: ProductStateStateInterface = {
  isProductsLoaded: false,
  productList: null,
  errors: null,
};
